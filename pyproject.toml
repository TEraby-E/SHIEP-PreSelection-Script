[project]
name = "shiep-preselection-script"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.40.0",
    "rich>=13.0.0",
    "pyyaml>=6.0",
]

[project.scripts]
shiep = "src.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
